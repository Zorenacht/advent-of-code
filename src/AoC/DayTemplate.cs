using FluentAssertions;

namespace AoC_YearPlaceholder;

public sealed class DayDayPlaceholder : Day
{
    [Puzzle(answer: null)]
    public void Part1()
    {
        int result = 0;
        var parse = Input;
        result.Should().Be(0);
    }

    [Puzzle(answer: null)]
    public void Part2()
    {
        int result = 0;
        var parse = Input;
        result.Should().Be(0);
    }
};